namespace itchio {
    // An itch.io profile
    struct Profile {}

    // double a number
    server fn double(
        // the number to double
        x: int64,
    ) -> (
        // the number, doubled
        y: int64,
    )

    server fn double(woops: double)

    server fn ping(a: int64)
}

namespace itchio {}